'use client'

// This component is no longer needed since we're using React Hook Form
// Keeping it for compatibility but it doesn't load any scripts
export default function GravityFormsScripts() {
  return null
}
